<%= form_for @photo, :url => photos_path, :html => { :multipart => true } do |f| %>
  <p>
    <%= hidden_field :user_id, :value => @user.id %>
    <%= f.file_field :photo %>
  </p>
  <p><%= f.submit "Submit" %></p>
<% end %>
